const recoverPassword = () => {
  return 'test recover password';
};

const sendEmailTest = () => {
  return 'Testing send email';
};

export const mailTemplates = Object.freeze({
  RECOVER_PASSWORD: recoverPassword,
  SEND_EMAIL_TEST: sendEmailTest,
});
